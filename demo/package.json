{
    "name": "near-example",
    "version": "0.1.0",
    "license": "(MIT OR Apache-2.0)",
    "scripts": {
        "build": "npm run clean && npm run build:css && cross-env BUILD_MODE=release rollup --config rollup.js",
        "start": "npm run clean && npm run build:css && run-p watch:css _dev",
        "build:css": "postcss src/styles/main.css --output public/css/main.css",
        "watch:css": "npm run build:css -- --watch",
        "clean": "rimraf ./public/wasm && rimraf ./public/css",
        "_dev": "cross-env BUILD_MODE=dev rollup --config rollup.js --watch"
    },
    "dependencies": {
        "@wasm-tool/rollup-plugin-rust": "^2.2.2"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.7",
        "cross-env": "^7.0.3",
        "cssnano": "^5.1.12",
        "dotenv": "^16.0.1",
        "npm-run-all": "^4.1.5",
        "postcss": "^8.4.14",
        "postcss-cli": "^9.1.0",
        "postcss-import": "^14.1.0",
        "postcss-reporter": "^7.0.5",
        "postcss-url": "^10.1.3",
        "rimraf": "^3.0.2",
        "rollup": "^2.75.6",
        "rollup-plugin-inject-process-env": "^1.3.1",
        "rollup-plugin-json": "^4.0.0",
        "rollup-plugin-livereload": "^2.0.5",
        "rollup-plugin-serve": "^1.1.0",
        "tailwindcss": "^3.1.3"
    }
}
